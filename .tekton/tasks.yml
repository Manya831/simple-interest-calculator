apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: test-task

spec:
  steps:
    - name: cleanup
      image: alpine
      script: |
        echo "Cleaning workspace"

    - name: run-tests
      image: python:3.9
      script: |
        nosetests
